function Product(id, name, type, screen, backCamera, frontCamera, image, desc, type) {
    this.id = id;
    this.name = name;
    this.type = type;
    this.screen = screen;
    this.backCamera = backCamera;
    this.frontCamera = frontCamera;
    this.image = image;
    this.desc = desc;
  }
